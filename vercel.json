{
  "projects": [
    {
      "src": "frontend/",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "bun build",
        "outputDirectory": "dist"
      }
    },
    {
      "src": "backend/",
      "use": "@vercel/node"
    }
  ]
}
